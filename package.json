{
  "name": "agenda-with-ai",
  "version": "1.0.0",
  "description": "API Rest para cadastro de contatos com Express, banco em memória e Swagger",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "mocha test/**/*.test.js --timeout 10000 --reporter json > report/test-results.json",
    "test-controller": "mocha test/rest/controller/**/*.test.js --timeout 10000 --reporter json > report/test-results.json",
    "test-external": "mocha test/rest/external/**/*.test.js --timeout 10000 --reporter json > report/test-results.json",
    "prettier": "prettier --write \"**/*.{js,json}\" --ignore-path .gitignore"
  },
  "dependencies": {
    "apollo-server-express": "^3.13.0",
    "bcryptjs": "^2.4.3",
    "chai": "^6.0.1",
    "dotenv": "^17.2.2",
    "express": "^4.21.2",
    "graphql": "^16.11.0",
    "jsonwebtoken": "^9.0.2",
    "mocha": "^11.7.1",
    "mochawesome": "^7.1.4",
    "sinon": "^21.0.0",
    "supertest": "^7.1.4",
    "swagger-ui-express": "^4.6.3"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.0.0",
    "prettier": "3.6.2"
  }
}
