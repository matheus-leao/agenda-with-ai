{
  "query": "mutation Register($name: String!, $password: String!) { register(name: $name, password: $password) { name token }}",
  "variables": {
    "name": "teste01",
    "password": "1234"
  }
}
